<h1 class='title'>Welcome to your BucketList!</h1>
<hr />

<%= link_to 'Start a new bucketList', new_list_path %>

<ul >
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, lists_path(list) %>
    </li>
  <% end %>
</ul>

