<h1>Index</h1>

<%= link_to 'Create a new List item', new_list_path %>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
      <%= link_to 'edit', edit_list_path(list) %>
      <%= link_to 'delete', [list], method: :delete %>
    </li>
  <% end %>
</ul>
